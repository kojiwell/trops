version: "3.9"
services:
  testbox:
    build: .
    hostname: testbox
    stdin_open: true
    tty: true
    environment:
      - USER=root
    volumes:
      - $PWD/../../:/trops
      - $PWD/tenvs:/tenvs
